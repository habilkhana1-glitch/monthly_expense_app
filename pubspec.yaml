name: monthly_expense_app
description: Monthly Expense Tracker
publish_to: 'none'

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.2.5+1
  path: ^1.9.1
  intl: ^0.18.1

flutter:
  uses-material-design: true
